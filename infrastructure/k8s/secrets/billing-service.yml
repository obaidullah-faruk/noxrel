apiVersion: v1
kind: Secret
metadata:
  name: billing-service-secrets
  namespace: platform
type: Opaque
stringData:
  SECRET_KEY: "dev-secret-key-change-in-production"
  DB_USER: "noxrel"
  DB_PASSWORD: "noxrel"
  # Replace with real Stripe test-mode keys before running
  STRIPE_SECRET_KEY: "sk_test_replace_me"
  STRIPE_WEBHOOK_SECRET: "whsec_replace_me"
  # Leave empty to use the built-in dev defaults (matches user-service dev key)
  JWT_PUBLIC_KEY: ""
